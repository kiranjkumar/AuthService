package com.java.authService.dtos;

public enum ResponseStatus {
    SUCCESS,
    FAILURE,
    ERROR,
    PENDING,
    INVALID_REQUEST,
    UNAUTHORIZED,
    FORBIDDEN,
    NOT_FOUND,
    CONFLICT,
    INTERNAL_SERVER_ERROR;
}
